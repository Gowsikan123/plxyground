# BLOCKERS

- None at the moment; basic route and UI scaffolding now implemented.
- Note: final production app store requirements need privacy/permissions text and EAS build config.
- CI: GitHub Actions smoke workflow has been removed. Re-enable when CI infrastructure is ready by creating `.github/workflows/ci.yml` that runs `npm test` in `backend/`.
