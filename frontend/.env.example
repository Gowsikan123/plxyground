EXPO_PUBLIC_API_BASE_URL=http://localhost:3011
# For Vercel web deployments, prefer setting BACKEND_URL in the Vercel project
# so /api/* requests can be proxied without hardcoding a production API host here.
